algoritmo "Arrays Vetores 1"
var
   minhanota: real
   notas: vetor[0..9] de real
inicio
      notas[0] <- 8
      minhanota <- notas[0]
      escreval(minhanota)
      escreval(notas[0])
fimalgoritmo