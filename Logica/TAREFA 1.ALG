algoritmo "semnome"
var
   fixo: real
   salario: real
inicio
   escreval("Quantos sistemas foram vendidos? ")
   leia(salario)
   fixo <- 500
   escreval("O salário do vendendor este mês é: R$",fixo + (50 * salario))
fimalgoritmo